steps:
- name: gcr.io/cloud-builders/docker
  args: [build, -f, demo/Dockerfile, -t, europe-west2-docker.pkg.dev/$PROJECT_ID/assessment-demo/pipeline:dev, .]
images: [europe-west2-docker.pkg.dev/$PROJECT_ID/assessment-demo/pipeline:dev]
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 900s
